"angle_of_branching", "stem_diameter", "fresh_yield",
"dry_yield", "starch", "dry_matter",
"height_at_first_branch", "root_weight", "flowering")
# Subset the data to include only these variables
data_subset <- data[variables]
# Step 1: Calculate the correlation matrix
cor_matrix <- cor(data_subset, use = "complete.obs")
# Step 2: Create a correlation plot using ggcorrplot
ggcorrplot(cor_matrix,
method = "circle",      # Correlation method for visualization
type = "lower",         # Show only the lower triangle
lab = TRUE,             # Display the correlation coefficient
title = "Correlation Matrix",
colors = c("red", "white", "blue"))  # Customize the color gradient
# Step 3: Alternatively, you can create a correlation plot using corrplot
corrplot(cor_matrix, method = "color",
col = colorRampPalette(c("red", "white", "blue"))(200),
addCoef.col = "black", # Add correlation coefficients
number.cex = 0.7,      # Control the size of the coefficients
tl.cex = 0.8,          # Text size for variable names
title = "Correlation Matrix Heatmap")
# Step 4: Create pairwise scatter plots for selected variable pairs
# You can modify this to plot any specific pair of variables
pairs(data_subset,
main = "Pairwise Scatter Plots",
pch = 21, bg = "lightblue",
col = "black")
# Optional: Plot individual scatterplots with regression lines (for one pair)
ggplot(data, aes(x = plant_height, y = fresh_yield)) +
geom_point() +
geom_smooth(method = "lm", se = FALSE, color = "blue") +
labs(title = "Scatter Plot of Plant Height vs Fresh Yield",
x = "Plant Height",
y = "Fresh Yield") +
theme_minimal()
# Assuming your data is in a data frame called `data`
# Load your dataset (replace 'your_data.csv' with the actual data file)
# data <- read.csv("your_data.csv")
data <- read.csv("varcor.csv")
# Select the relevant columns for correlation analysis
variables <-	c("ANGBR9",	"BRNHB9",	"BRNHT9",	"BRNHT6",	"BRNLEV9",	"DM",	"DYLD", "FYLD",	"HI",	"LODG",	"NOHAV",	"PLTHT9",	"PLTHT6",	"PPSTD9",	"PT",	"RTNO",	"RTWT",	"SHTWT",	"STARCH",	"STMDI9",	"TYLD"	"VR)
#variables <- c("plant_height", "number_of_branches", "level_of_branching",
"dry_yield", "starch", "dry_matter",
Subset the data to include only these variables
# Step 1: Calculate the correlation matrix
# Step 2: Create a correlation plot using ggcorrplot
method = "circle",      # Correlation method for visualization
lab = TRUE,             # Display the correlation coefficient
colors = c("red", "white", "blue"))  # Customize the color gradient
# Step 3: Alternatively, you can create a correlation plot using corrplot
col = colorRampPalette(c("red", "white", "blue"))(200),
number.cex = 0.7,      # Control the size of the coefficients
title = "Correlation Matrix Heatmap")
# Step 4: Create pairwise scatter plots for selected variable pairs
# Step 4: Create pairwise scatter plots for selected variable pairs
# You can modify this to plot any specific pair of variables
pairs(data_subset,
pch = 21, bg = "lightblue",
# Optional: Plot individual scatterplots with regression lines (for one pair)
geom_point() +
labs(title = "Scatter Plot of Plant Height vs Fresh Yield",
y = "Fresh Yield") +
Subset the data to include only these variables
setwd("/Users/abiodunolayinka/Documents/Abiodun Olayinka/DATA ANALYSIS/correlation")
# Assuming your data is in a data frame called `data`
# Load your dataset (replace 'your_data.csv' with the actual data file)
# data <- read.csv("your_data.csv")
data <- read.csv("varcor.csv")
# Select the relevant columns for correlation analysis
variables <-	c("ANGBR9",	"BRNHB9",	"BRNHT9",	"BRNHT6",	"BRNLEV9",	"DM",	"DYLD", "FYLD",	"HI",	"LODG",	"NOHAV",	"PLTHT9",	"PLTHT6",	"PPSTD9",	"PT",	"RTNO",	"RTWT",	"SHTWT",	"STARCH",	"STMDI9",	"TYLD"	"VR)
#variables <- c("plant_height", "number_of_branches", "level_of_branching",
variables <- c("ANGBR9", "BRNHB9", "BRNHT9", "BRNHT6", "BRNLEV9", "DM", "DYLD",
"FYLD", "HI", "LODG", "NOHAV", "PLTHT9", "PLTHT6", "PPSTD9",
"RTNO", "RTWT", "SHTWT", "STARCH", "STMDI9", "TYLD")
# Subset the data to include only these variables
data_subset <- data[variables]
# Step 1: Calculate the correlation matrix
cor_matrix <- cor(data_subset, use = "complete.obs")
# Step 2: Create a correlation plot using ggcorrplot
ggcorrplot(cor_matrix,
method = "circle",      # Correlation method for visualization
type = "lower",         # Show only the lower triangle
lab = TRUE,             # Display the correlation coefficient
title = "Correlation Matrix",
colors = c("red", "white", "blue"))  # Customize the color gradient
# Step 3: Alternatively, you can create a correlation plot using corrplot
corrplot(cor_matrix, method = "color",
col = colorRampPalette(c("red", "white", "blue"))(200),
addCoef.col = "black", # Add correlation coefficients
number.cex = 0.7,      # Control the size of the coefficients
tl.cex = 0.8,          # Text size for variable names
title = "Correlation Matrix Heatmap")
# Step 4: Create pairwise scatter plots for selected variable pairs
# You can modify this to plot any specific pair of variables
pairs(data_subset,
main = "Pairwise Scatter Plots",
pch = 21, bg = "lightblue",
col = "black")
# Optional: Plot individual scatterplots with regression lines (for one pair)
ggplot(data, aes(x = plant_height, y = fresh_yield)) +
geom_point() +
geom_smooth(method = "lm", se = FALSE, color = "blue") +
labs(title = "Scatter Plot of Plant Height vs Fresh Yield",
x = "Plant Height",
y = "Fresh Yield") +
theme_minimal()
# Assuming your data is in a data frame called `data`
# Load your dataset (replace 'your_data.csv' with the actual data file)
# data <- read.csv("your_data.csv")
data <- read.csv("varcor.csv")
# Select the relevant columns for correlation analysis
variables <-	c("ANGBR9",	"BRNHB9",	"BRNHT9",	"BRNHT6",	"BRNLEV9",	"DM",	"DYLD", "FYLD",	"HI",	"LODG",	"NOHAV",	"PLTHT9",	"PLTHT6",	"PPSTD9",	"PT",	"RTNO",	"RTWT",	"SHTWT",	"STARCH",	"STMDI9",	"TYLD"	"VR)
#variables <- c("plant_height", "number_of_branches", "level_of_branching",
"dry_yield", "starch", "dry_matter",
Subset the data to include only these variables
# Step 1: Calculate the correlation matrix
# Step 2: Create a correlation plot using ggcorrplot
method = "circle",      # Correlation method for visualization
lab = TRUE,             # Display the correlation coefficient
colors = c("red", "white", "blue"))  # Customize the color gradient
# Step 3: Alternatively, you can create a correlation plot using corrplot
col = colorRampPalette(c("red", "white", "blue"))(200),
number.cex = 0.7,      # Control the size of the coefficients
title = "Correlation Matrix Heatmap")
# Step 4: Create pairwise scatter plots for selected variable pairs
# Step 4: Create pairwise scatter plots for selected variable pairs
# You can modify this to plot any specific pair of variables
pairs(data_subset,
pch = 21, bg = "lightblue",
# Optional: Plot individual scatterplots with regression lines (for one pair)
geom_point() +
labs(title = "Scatter Plot of Plant Height vs Fresh Yield",
y = "Fresh Yield") +
Subset the data to include only these variables
# Remove all objects from the environment
rm(list = ls())
setwd("/Users/abiodunolayinka/Documents/Abiodun Olayinka/DATA ANALYSIS/correlation/ikenne")
data <- read.csv("IKENNE META.csv")
# Check if the matrix is loaded correctly
print(data)
# Extract the numeric part of the matrix (as the first column and row may be labels)
# Converting data to matrix
cor_matrix <- as.matrix(data[, -1])  # Exclude the first column, assuming it's row names
# Set row names (if needed)
rownames(cor_matrix) <- data[[1]]  # Set the first column as row names
# Print the correlation matrix to verify
print(cor_matrix)
# Create the correlation plot using corrplot
# This will display both the upper and lower triangles of the matrix
corrplot(cor_matrix,
method = "circle",      # Use circles to represent correlation
type = "full",          # Display both upper and lower triangles
order = "original",     # Keep the original order of variables
tl.col = "black",       # Color for text labels
tl.srt = 45,            # Rotate text labels by 45 degrees
col = colorRampPalette(c("red", "white", "blue"))(200),  # Color scale from red to blue
addCoef.col = "black",  # Add correlation coefficient numbers
number.cex = 0.8,       # Size of the correlation coefficient text
cl.pos = "r")           # Color legend position (right side)
setwd("/Users/abiodunolayinka/Documents/Abiodun Olayinka/DATA ANALYSIS/correlation/3 env")
data <- read.csv("3 ENV.csv")
# Check if the matrix is loaded correctly
print("Loaded Data:")
print(data)
# Extract the numeric part of the matrix (assuming the first column contains row labels)
cor_matrix <- as.matrix(data[, -1])  # Exclude the first column, which may be row names
# Set row names using the first column (if applicable)
rownames(cor_matrix) <- data[[1]]
# Print the correlation matrix to verify
print("Correlation Matrix:")
print(cor_matrix)
# Plot the correlation matrix using corrplot
corrplot(cor_matrix,
method = "circle",        # Circles represent correlation
type = "lower",           # Show only lower triangle
order = "original",       # Keep original variable order
tl.col = "black",         # Text label color
tl.srt = 45,              # Rotate text labels by 45 degrees
col = colorRampPalette(c("red", "white", "blue"))(200),  # Color scale
addCoef.col = "black",    # Add correlation coefficient numbers
number.cex = 0.8,         # Coefficient text size
cl.pos = "r")             # Color legend position
# Function to process and plot a correlation matrix from an Excel sheet
plot_correlation_matrix <- function(file_path, sheet_num) {
# Read the data from the Excel file (data from specified sheet)
#data <- read_excel(file_path, sheet = sheet_num)
data <- read.csv("3 ENV.csv")
# Check if the matrix is loaded correctly
print("Loaded Data:")
print(data)
# Extract the numeric part of the matrix (assuming the first column contains row labels)
cor_matrix <- as.matrix(data[, -1])  # Exclude the first column, which may be row names
# Set row names using the first column (if applicable)
rownames(cor_matrix) <- data[[1]]
# Print the correlation matrix to verify
print("Correlation Matrix:")
print(cor_matrix)
# Plot the correlation matrix using corrplot
corrplot(cor_matrix,
method = "circle",        # Circles represent correlation
type = "lower",           # Show only lower triangle
order = "original",       # Keep original variable order
tl.col = "black",         # Text label color
tl.srt = 45,              # Rotate text labels by 45 degrees
col = colorRampPalette(c("red", "white", "blue"))(200),  # Color scale
addCoef.col = "black",    # Add correlation coefficient numbers
number.cex = 0.8,         # Coefficient text size
cl.pos = "r")             # Color legend position
}
print(data)
# Extract the numeric part of the matrix (assuming the first column contains row labels)
cor_matrix <- as.matrix(data[, -1])  # Exclude the first column, which may be row names
# Set row names using the first column (if applicable)
rownames(cor_matrix) <- data[[1]]
# Print the correlation matrix to verify
print("Correlation Matrix:")
print(cor_matrix)
# Check if the matrix values are within the [-1, 1] range
if (any(cor_matrix < -1 | cor_matrix > 1)) {
# Clip values that are slightly out of range
cor_matrix[cor_matrix < -1] <- -1
cor_matrix[cor_matrix > 1] <- 1
print("Adjusted Correlation Matrix (values clamped to [-1, 1]):")
print(cor_matrix)
}
# Plot the correlation matrix using corrplot
corrplot(cor_matrix,
method = "circle",        # Circles represent correlation
type = "lower",           # Show only lower triangle
order = "original",       # Keep original variable order
tl.col = "black",         # Text label color
tl.srt = 45,              # Rotate text labels by 45 degrees
col = colorRampPalette(c("red", "white", "blue"))(200),  # Color scale
addCoef.col = "black",    # Add correlation coefficient numbers
number.cex = 0.8,         # Coefficient text size
cl.pos = "r")             # Color legend position
# Function to process and plot a correlation matrix from an Excel sheet
plot_correlation_matrix <- function(file_path, sheet_num) {
# Read the data from the Excel file (data from specified sheet)
data <- read_excel(file_path, sheet = sheet_num)
# Check if the matrix is loaded correctly
print("Loaded Data:")
print(data)
# Extract the numeric part of the matrix (assuming the first column contains row labels)
cor_matrix <- as.matrix(data[, -1])  # Exclude the first column, which may be row names
# Set row names using the first column (if applicable)
rownames(cor_matrix) <- data[[1]]
# Print the correlation matrix to verify
print("Correlation Matrix:")
print(cor_matrix)
# Check if the matrix values are within the [-1, 1] range
if (any(cor_matrix < -1 | cor_matrix > 1)) {
# Clip values that are slightly out of range
cor_matrix[cor_matrix < -1] <- -1
cor_matrix[cor_matrix > 1] <- 1
print("Adjusted Correlation Matrix (values clamped to [-1, 1]):")
print(cor_matrix)
}
# Plot the correlation matrix using corrplot
corrplot(cor_matrix,
method = "circle",        # Circles represent correlation
type = "lower",           # Show only lower triangle
order = "original",       # Keep original variable order
tl.col = "black",         # Text label color
tl.srt = 45,              # Rotate text labels by 45 degrees
col = colorRampPalette(c("red", "white", "blue"))(200),  # Color scale
addCoef.col = "black",    # Add correlation coefficient numbers
number.cex = 0.8,         # Coefficient text size
cl.pos = "r")             # Color legend position
}
# Read the data from the Excel file (assuming data is in the first sheet)
data <- read_excel(file_path, sheet = 1)
# Check if the matrix is loaded correctly
print(data)
# Extract the numeric part of the matrix (as the first column and row may be labels)
# Converting data to matrix
cor_matrix <- as.matrix(data[, -1])  # Exclude the first column, assuming it's row names
# Set row names (if needed)
rownames(cor_matrix) <- data[[1]]  # Set the first column as row names
# Print the correlation matrix to verify
print(cor_matrix)
# Create the correlation plot using corrplot
# This will display only the lower triangle of the matrix
corrplot(cor_matrix,
method = "circle",      # Use circles to represent correlation
type = "lower",         # Display only the lower triangle
order = "original",     # Keep the original order of variables
tl.col = "black",       # Color for text labels
tl.srt = 45,            # Rotate text labels by 45 degrees
col = colorRampPalette(c("red", "white", "blue"))(200),  # Color scale from red to blue
addCoef.col = "black",  # Add correlation coefficient numbers
number.cex = 0.8,       # Size of the correlation coefficient text
cl.pos = "r")           # Color legend position (right side)
data <- read.csv("3 ENV.csv")
# Check if the matrix is loaded correctly
print("Loaded Data:")
print(data)
# Extract the numeric part of the matrix (assuming the first column contains row labels)
cor_matrix <- as.matrix(data[, -1])  # Exclude the first column, which may be row names
# Set row names using the first column (if applicable)
rownames(cor_matrix) <- data[[1]]
# Print the correlation matrix to verify
print("Correlation Matrix:")
print(cor_matrix)
# Plot the correlation matrix using corrplot
corrplot(cor_matrix,
method = "circle",        # Circles represent correlation
type = "lower",           # Show only lower triangle
order = "original",       # Keep original variable order
tl.col = "black",         # Text label color
tl.srt = 45,              # Rotate text labels by 45 degrees
col = colorRampPalette(c("red", "white", "blue"))(200),  # Color scale
addCoef.col = "black",    # Add correlation coefficient numbers
number.cex = 0.8,         # Coefficient text size
cl.pos = "r")             # Color legend position
# Function to process and plot a correlation matrix from an Excel sheet
plot_correlation_matrix <- function(file_path, sheet_num) {
# Read the data from the Excel file (data from specified sheet)
#data <- read_excel(file_path, sheet = sheet_num)
data <- read.csv("3 ENV.csv")
# Check if the matrix is loaded correctly
print("Loaded Data:")
print(data)
# Extract the numeric part of the matrix (assuming the first column contains row labels)
cor_matrix <- as.matrix(data[, -1])  # Exclude the first column, which may be row names
# Set row names using the first column (if applicable)
rownames(cor_matrix) <- data[[1]]
# Print the correlation matrix to verify
print("Correlation Matrix:")
print(cor_matrix)
# Plot the correlation matrix using corrplot
corrplot(cor_matrix,
method = "circle",        # Circles represent correlation
type = "lower",           # Show only lower triangle
order = "original",       # Keep original variable order
tl.col = "black",         # Text label color
tl.srt = 45,              # Rotate text labels by 45 degrees
col = colorRampPalette(c("red", "white", "blue"))(200),  # Color scale
addCoef.col = "black",    # Add correlation coefficient numbers
number.cex = 0.8,         # Coefficient text size
cl.pos = "r")             # Color legend position
}
# Plot the correlation matrix using corrplot
corrplot(cor_matrix,
method = "circle",        # Circles represent correlation
type = "lower",           # Show only lower triangle
order = "original",       # Keep original variable order
tl.col = "black",         # Text label color
tl.srt = 45,              # Rotate text labels by 45 degrees
col = colorRampPalette(c("red", "white", "blue"))(200),  # Color scale
addCoef.col = "black",    # Add correlation coefficient numbers
number.cex = 0.8,         # Coefficient text size
cl.pos = "r")             # Color legend position
print(data)
# Extract the numeric part of the matrix (assuming the first column contains row labels)
cor_matrix <- as.matrix(data[, -1])  # Exclude the first column, which may be row names
# Set row names using the first column (if applicable)
rownames(cor_matrix) <- data[[1]]
# Print the correlation matrix to verify
print("Correlation Matrix:")
print(cor_matrix)
# Check if the matrix values are within the [-1, 1] range
if (any(cor_matrix < -1 | cor_matrix > 1)) {
# Clip values that are slightly out of range
cor_matrix[cor_matrix < -1] <- -1
cor_matrix[cor_matrix > 1] <- 1
print("Adjusted Correlation Matrix (values clamped to [-1, 1]):")
print(cor_matrix)
}
# Plot the correlation matrix using corrplot
corrplot(cor_matrix,
method = "circle",        # Circles represent correlation
type = "lower",           # Show only lower triangle
order = "original",       # Keep original variable order
tl.col = "black",         # Text label color
tl.srt = 45,              # Rotate text labels by 45 degrees
col = colorRampPalette(c("red", "white", "blue"))(200),  # Color scale
addCoef.col = "black",    # Add correlation coefficient numbers
number.cex = 0.8,         # Coefficient text size
cl.pos = "r")             # Color legend position
# Plot the correlation matrix using corrplot
corrplot(cor_matrix,
method = "circle",        # Circles represent correlation
type = "lower",           # Show only lower triangle
order = "original",       # Keep original variable order
tl.col = "black",         # Text label color
tl.srt = 45,              # Rotate text labels by 45 degrees
col = colorRampPalette(c("red", "white", "blue"))(200),  # Color scale
addCoef.col = "black",    # Add correlation coefficient numbers
number.cex = 0.8,         # Coefficient text size
cl.pos = "r")             # Color legend position
# Check if the matrix is loaded correctly
print(data)
# Extract the numeric part of the matrix (as the first column and row may be labels)
# Converting data to matrix
cor_matrix <- as.matrix(data[, -1])  # Exclude the first column, assuming it's row names
# Set row names (if needed)
rownames(cor_matrix) <- data[[1]]  # Set the first column as row names
# Print the correlation matrix to verify
print(cor_matrix)
# Create the correlation plot using corrplot
# This will display only the lower triangle of the matrix
corrplot(cor_matrix,
method = "circle",      # Use circles to represent correlation
type = "lower",         # Display only the lower triangle
order = "original",     # Keep the original order of variables
tl.col = "black",       # Color for text labels
tl.srt = 45,            # Rotate text labels by 45 degrees
col = colorRampPalette(c("red", "white", "blue"))(200),  # Color scale from red to blue
addCoef.col = "black",  # Add correlation coefficient numbers
number.cex = 0.8,       # Size of the correlation coefficient text
cl.pos = "r")           # Color legend position (right side)
setwd("/Users/abiodunolayinka/Documents/Abiodun Olayinka/DATA ANALYSIS/correlation/ikenne")
rm(list = ls())
data <- read.csv("IKENNE META.csv")
# Check if the matrix is loaded correctly
print(data)
# Extract the numeric part of the matrix (as the first column and row may be labels)
# Converting data to matrix
cor_matrix <- as.matrix(data[, -1])  # Exclude the first column, assuming it's row names
# Set row names (if needed)
rownames(cor_matrix) <- data[[1]]  # Set the first column as row names
# Print the correlation matrix to verify
print(cor_matrix)
# Create the correlation plot using corrplot
# This will display only the lower triangle of the matrix
corrplot(cor_matrix,
method = "circle",      # Use circles to represent correlation
type = "lower",         # Display only the lower triangle
order = "original",     # Keep the original order of variables
tl.col = "black",       # Color for text labels
tl.srt = 45,            # Rotate text labels by 45 degrees
col = colorRampPalette(c("red", "white", "blue"))(200),  # Color scale from red to blue
addCoef.col = "black",  # Add correlation coefficient numbers
number.cex = 0.8,       # Size of the correlation coefficient text
cl.pos = "r")           # Color legend position (right side)
# Check if the matrix is loaded correctly
print(data)
# Extract the numeric part of the matrix (as the first column and row may be labels)
# Converting data to matrix
cor_matrix <- as.matrix(data[, -1])  # Exclude the first column, assuming it's row names
# Set row names (if needed)
rownames(cor_matrix) <- data[[1]]  # Set the first column as row names
# Print the correlation matrix to verify
print(cor_matrix)
# Create the correlation plot using corrplot
# This will display only the lower triangle of the matrix
corrplot(cor_matrix,
method = "circle",      # Use circles to represent correlation
type = "lower",         # Display only the lower triangle
order = "original",     # Keep the original order of variables
tl.col = "black",       # Color for text labels
tl.srt = 45,            # Rotate text labels by 45 degrees
col = colorRampPalette(c("red", "white", "blue"))(200),  # Color scale from red to blue
addCoef.col = "black",  # Add correlation coefficient numbers
number.cex = 0.8,       # Size of the correlation coefficient text
cl.pos = "r",           # Color legend position (right side)
title = "Correlation Plot Ikenne Trial",   # Title of the plot
mar = c(0,0,2,0))       # Adjusting margin to fit the title at the top center
rm(list = ls())
data <- read.csv("ONNE META.csv")
setwd("/Users/abiodunolayinka/Documents/Abiodun Olayinka/DATA ANALYSIS/correlation/onne")
data <- read.csv("ONNE META.csv")
# Check if the matrix is loaded correctly
print(data)
# Extract the numeric part of the matrix (as the first column and row may be labels)
# Converting data to matrix
cor_matrix <- as.matrix(data[, -1])  # Exclude the first column, assuming it's row names
# Set row names (if needed)
rownames(cor_matrix) <- data[[1]]  # Set the first column as row names
# Print the correlation matrix to verify
print(cor_matrix)
# Create the correlation plot using corrplot
# This will display only the lower triangle of the matrix
corrplot(cor_matrix,
method = "circle",      # Use circles to represent correlation
type = "lower",         # Display only the lower triangle
order = "original",     # Keep the original order of variables
tl.col = "black",       # Color for text labels
tl.srt = 45,            # Rotate text labels by 45 degrees
col = colorRampPalette(c("red", "white", "blue"))(200),  # Color scale from red to blue
addCoef.col = "black",  # Add correlation coefficient numbers
number.cex = 0.8,       # Size of the correlation coefficient text
cl.pos = "r",           # Color legend position (right side)
title = "Correlation Plot Onne Trial",   # Title of the plot
mar = c(0,0,2,0))       # Adjusting margin to fit the title at the top center
setwd("/Users/abiodunolayinka/Documents/Abiodun Olayinka/DATA ANALYSIS/correlation/mokwa")
rm(list = ls())
data <- read.csv("MOKWA META.csv")
# Check if the matrix is loaded correctly
print(data)
# Extract the numeric part of the matrix (as the first column and row may be labels)
# Converting data to matrix
cor_matrix <- as.matrix(data[, -1])  # Exclude the first column, assuming it's row names
# Set row names (if needed)
rownames(cor_matrix) <- data[[1]]  # Set the first column as row names
# Print the correlation matrix to verify
print(cor_matrix)
# Create the correlation plot using corrplot
# This will display only the lower triangle of the matrix
corrplot(cor_matrix,
method = "circle",      # Use circles to represent correlation
type = "lower",         # Display only the lower triangle
order = "original",     # Keep the original order of variables
tl.col = "black",       # Color for text labels
tl.srt = 45,            # Rotate text labels by 45 degrees
col = colorRampPalette(c("red", "white", "blue"))(200),  # Color scale from red to blue
addCoef.col = "black",  # Add correlation coefficient numbers
number.cex = 0.8,       # Size of the correlation coefficient text
cl.pos = "r",           # Color legend position (right side)
title = "Correlation Plot Mokwa Trial",   # Title of the plot
mar = c(0,0,2,0))       # Adjusting margin to fit the title at the top center
